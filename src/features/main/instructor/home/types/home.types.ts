/**
 * The instructor home cards render the shared course card model. `Course` stays as the
 * local name so the existing components keep compiling unchanged.
 */
export type { CourseCardModel as Course } from "@/shared/courses";
